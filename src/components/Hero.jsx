import React from "react";

const Hero = () => {
    return (
        <div className="hero flex border-2 w-100 h-auto justify-center items-center">
            <img src="images/unnamed.jpg" className="object-cover" />
        </div>
    );
    //add a div below it for a small quote?
};

export default Hero;
